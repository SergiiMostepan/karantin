import React from "react";
import styles from "./Notification.module.css";

const Notify = () => (
  <div className={styles.container}>
    <p>Запись добавленна</p>
  </div>
);

export default Notify;
